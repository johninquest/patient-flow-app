# ===========================================
# PATIENT FLOW CLIENT ENVIRONMENT CONFIGURATION
# ===========================================
# Copy this file to .env and fill in the values.
# Only browser-safe variables belong here. Secrets must never be added.

# ---------- API URL ----------
# Base URL of the Patient Flow API (must be accessible from the browser).
VITE_API_URL=http://localhost:3000
